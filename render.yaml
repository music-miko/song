services:
  - type: worker                      # Not "web"
    name: music-bot
    env: docker
    plan: pro                         # or pro max, optional in most cases
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    region: singapore                # or oregon, frankfurt, etc.
